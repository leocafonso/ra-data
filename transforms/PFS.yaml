# PFS transforms - Pin Function Select registers
# Delete useless enums (bit_size 1)
# Rename block to PFS (normalize from PFS_A, PFS_B)

transforms:
  - !DeleteEnums
    from: .*
    bit_size: 1
    keep_desc: true
  - !DeleteFieldsets
    from: .*
    useless: true
